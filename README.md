# ETL Pipeline для аналитики данных

## Описание проекта

Проект представляет собой ETL-пайплайн для загрузки данных из различных источников в аналитическое хранилище данных.

В рамках проекта реализованы:

* извлечение данных из файлов различных форматов (CSV, JSON, Excel, XML);
* очистка и валидация данных;
* контроль качества данных (Data Quality);
* загрузка данных в PostgreSQL;
* выполнение аналитических SQL-запросов;
* логирование ошибок обработки данных.

---

## Структура проекта

```text
project/
├── src/
│   ├── config.py
│   ├── extract.py
│   ├── transform.py
│   ├── load.py
│   └── main.py
│
├── ddl/
│   └── schema.sql
│
├── sql/
│   └── analytics.sql
│
├── data/
│   ├── customers.csv
│   ├── orders.json
│   ├── products.xlsx
│   ├── payments.csv
│   └── events.xml
│
├── requirements.txt
└── README.md
```

### Назначение основных модулей

| Файл            | Описание                        |
| --------------- | ------------------------------- |
| `extract.py`    | Извлечение данных из источников |
| `transform.py`  | Очистка и преобразование данных |
| `load.py`       | Загрузка данных в DWH           |
| `main.py`       | Запуск ETL-процесса             |
| `schema.sql`    | Создание структуры хранилища    |
| `analytics.sql` | Аналитические SQL-запросы       |

---

## Архитектурные решения

### Архитектура ETL

В проете используется Galaxy Schema, так как в модели присутствуют несколько таблиц фактов, которые используют общие таблицы измерений.

Это позволяет гибко добавлять новые бизнес-процессы (факты) без дублирования справочников, но при этом изменение структуры общего измерения требует внимания, чтобы не затронуть все связанные факты.

<img width="1535" height="1024" alt="f35d60b1-6f6e-4fd0-8bee-44904423a27d" src="https://github.com/user-attachments/assets/37b1c753-1600-461a-8fbf-a19ee357b18d" />


### Контроль качества данных

На этапе трансформации выполняются проверки данных на корректность и полноту.

Контролируются следующие типы ошибок:

* дублирующиеся записи;
* некорректные email-адреса;
* некорректные номера телефонов;
* отсутствующие обязательные поля;
* ошибки форматов дат;
* некорректные суммы платежей;
* нарушения ссылочной целостности.

Все обнаруженные ошибки фиксируются в журнале качества данных для последующего анализа.

---

## Используемые технологии

| Технология      | Назначение              |
| --------------- | ----------------------- |
| Python 3.10+    | Реализация ETL-процесса |
| Pandas          | Обработка данных        |
| PostgreSQL      | Хранилище данных        |
| SQLAlchemy      | Работа с БД             |
| psycopg2-binary | PostgreSQL-драйвер      |
| openpyxl        | Чтение Excel-файлов     |

---

## Запуск проекта

### 1. Клонирование репозитория

```bash
git clone https://github.com/fedinaa/project.git
cd project
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Создание базы данных

```bash
psql -U postgres -c "CREATE DATABASE mosmusic;"
```

### 4. Создание структуры хранилища

```bash
psql -U postgres -d mosmusic -f ddl/schema.sql
```

### 5. Настройка подключения к БД

В файле `src/config.py`:

```python
DB_CONFIG = {
    "host": "localhost",
    "port": "5432",
    "database": "mosmusic",
    "user": "postgres",
    "password": "postgres"
}
```

### 7. Запуск ETL-процесса

```bash
cd src
python main.py
```

### 8. Выполнение аналитических запросов

```bash
psql -U postgres -d mosmusic -f sql/analytics.sql
```
